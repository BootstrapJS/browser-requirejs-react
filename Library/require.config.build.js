/* global require */
require.config({
    "paths": {
        // Use preminified production build of reactjs
        // This one is a lot smaller than a custom minified one :)
        "react": "../bower_components/react/react-with-addons.min"
    }
});
